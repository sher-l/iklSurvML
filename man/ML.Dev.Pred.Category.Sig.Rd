% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ML.Dev.Pred.Category.Sig.R, R/backward-compat.R
\name{ML.Dev.Pred.Category.Sig}
\alias{ML.Dev.Pred.Category.Sig}
\alias{ml_dev_pred_category_sig}
\title{Developing the optimal predictive model for dichotomous variables with machine learning algorithms}
\usage{
ML.Dev.Pred.Category.Sig(
  train_data,
  list_train_vali_Data,
  candidate_genes = NULL,
  methods = NULL,
  seed = 5201314,
  cores_for_parallel = 12,
  positive_class = "Y",
  feature_alignment = c("strict", "intersection"),
  tune_profile = c("standard", "exhaustive"),
  cv_folds = 5,
  cv_repeats = 3
)

ml_dev_pred_category_sig(...)
}
\arguments{
\item{train_data}{The training data with the 'ID' and 'Var' as the first two columns. Starting in the third column are the variables used to construct the model. 'Var' is the target predictor variable for constructing the model and contains only Y or N.}

\item{list_train_vali_Data}{A list containing the training data and validation data. All validation data have the same data form as the training data.}

\item{candidate_genes}{The candidate variables used for constructing the predictive model.}

\item{methods}{Algorithms for developing the predictive model: 'nb', 'svmRadialWeights', 'rf', 'kknn', 'adaboost', 'LogitBoost', and 'cancerclass'.}

\item{seed}{Random seed. Defaults to 5201314.}

\item{cores_for_parallel}{Number of worker cores for parallel operation.}

\item{positive_class}{The class treated as the positive/event class for ROC/AUC. Defaults to 'Y'.}

\item{feature_alignment}{How candidate genes are aligned across cohorts. 'strict' requires all candidate genes to be present in every dataset; 'intersection' trains only on genes shared by all cohorts, with a warning for drops.}

\item{tune_profile}{Tuning grid size profile. 'standard' uses a bounded grid; 'exhaustive' restores the larger legacy grid.}

\item{cv_folds}{Number of folds for repeated cross-validation. Defaults to 5 and is capped by the smallest class count.}

\item{cv_repeats}{Number of repeated cross-validation rounds. Defaults to 3.}

\item{...}{Arguments passed to `ML.Dev.Pred.Category.Sig()`.}
}
\value{
A list containing predictive models, AUCs, ROCs, candidate variables, preprocessing metadata, and positive-class metadata.
}
\description{
A function can be used to develop predictive models for dichotomous variables with seven machine learning algorithms.
}
\examples{
\dontrun{
# See package vignettes or tests for runnable examples with optional engines.
}
}
